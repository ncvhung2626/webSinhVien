/**
 * config.js - Cấu hình API chung cho trang Admin
 * Khi dùng Live Server, giữ nguyên giá trị này.
 * Khi deploy, đổi thành '' (rỗng) để dùng relative path.
 */
const API_BASE_URL = '/api/admin/';
